#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strpbrk - print string.
 * @s: the string
 * @accept: the string
 * Return: String.
 */
char *_strpbrk(char *s, char *accept)
{
	return (s, accept);
}
