#include <stdio.h>

/**
 * main - print alphabet in a reverse orde.
 *
 * Reture: Always 0 (Success)
 */
int main(void)
{
	char c;

	for (c = 'z'; C >= 'a'; c--)
	{
		putchar(c);
	}
	puter('\n');

	return (0);
}
