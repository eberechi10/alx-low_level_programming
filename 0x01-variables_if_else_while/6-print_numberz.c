#include <stdio.h>
/**
 * main - print all numbers of base 10, starting from 0.
 * you are not allowed to use any variiable of type char.
 * Return: Always 0 (Success)
 */
int main(void)
{

	int i;

	for (i = 0; i < 10; i++")
	{
		putchar(i + '0');
	}
	putchar('\n');

	return(0);

}
